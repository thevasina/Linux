1.	Создать файл file1 и наполнить его произвольным содержимым. Скопировать его в file2. Создать символическую ссылку file3 на file1. Создать жёсткую ссылку file4 на file1. Посмотреть, какие inode у файлов. Удалить file1. Что стало с остальными созданными файлами? Попробовать вывести их на экран.
- cat > file1
- cat file1 > file2
- ln -s file1 file3
- ln file1 file4
- скрин 1 (скрин с inod файлов и ссылок)
- rm file1
- файлы file2 и file4 открываются, file3 - не существует (так как это символическая ссылка)

2.	Дать созданным файлам другие, произвольные имена. Создать новую символическую ссылку. Переместить ссылки в другую директорию.
- mv file2 newfile2
- mv file3 newfile3
- mv file4 newfile4
- ln -s newfile2 linkfile2
- mkdir newdir
- mv linkfile2 newfile2 newfile3 newfile4 newdir

3.
Создать два произвольных файла. Первому присвоить права на чтение и запись для владельца и группы, только на чтение — для всех. Второму присвоить права на чтение и запись только для владельца. Сделать это в численном и символьном виде.
- cat > first
- echo second > second
- chmod ug=rw,o=r first
- chmod 600 second (группе и остальным не давала никакх прав, не совсем понятно из задания надо дать нулевые права или только на чтение)

4.
* Создать группу developer и нескольких пользователей, входящих в неё. Создать директорию для совместной работы. Сделать так, чтобы созданные одними пользователями файлы могли изменять другие пользователи этой группы
- sudo adduser dev1
- sudo adduser dev2
- sudo adduser dev3
- sudo addgroup developer
- sudo gpasswd -M dev1,dev2,dev3 developer
- mkdir dev_work
- sudo chgrp developer dev_work/
- sudo chmod g+s dev_work/

5.
* Создать в директории для совместной работы поддиректорию для обмена файлами, но чтобы удалять файлы могли только их создатели.
- mkdir common_work
- drwxrwsr-x 2 thevasina developer 4096 фев  3 23:33 common_work (группа developer)
- chmod +t common_work/
- drwxrwxr-t 2 thevasina developer 4096 фев  3 23:33 common_work (в правах появилось -t)

6.
* Создать директорию, в которой есть несколько файлов. Сделать так, чтобы открыть файлы можно было, только зная имя файла, а через ls список файлов посмотреть было нельзя.
- mkdir test
- cd test/
- touch test1
- touch test2
- chmod a-r test/
